{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "playground",
            "type": "cppvsdbg",
            "cwd": "${workspaceFolder}",
            "request": "launch",
            "program": "${workspaceFolder}/build/lotus/playground.exe"
        }
    ]
}