{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "debug",
            "type": "cppvsdbg",
            "cwd": "${workspaceFolder}",
            "request": "launch",
            "program": "${workspaceFolder}/build/lotus.exe",
            "sourceFileMap": {
                ".": "${workspaceFolder}"
            }
        }
    ]
}